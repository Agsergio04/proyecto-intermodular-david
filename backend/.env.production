# ============================================
# CONFIGURACIÓN BACKEND - ENTORNO PRODUCCIÓN (RENDER)
# ============================================
# Este archivo es SOLO DE REFERENCIA
# Las variables reales se configuran en:
# Render Dashboard > Tu Servicio > Environment > Environment Variables
# NO contiene valores reales para evitar exponerlos en el repositorio

# Entorno de ejecución
# Para RENDER siempre usar 'production'
NODE_ENV=production

# Puerto del servidor
# RENDER usa por defecto el puerto 10000
# No modificar a menos que Render lo requiera
PORT=10000

# URI de conexión a MongoDB Atlas
# Obtener en:
# 1. Crear cuenta en https://www.mongodb.com/cloud/atlas
# 2. Crear un cluster gratuito (M0)
# 3. Database Access > Add New Database User (crear usuario y contraseña)
# 4. Network Access > Add IP Address > Allow Access from Anywhere (0.0.0.0/0)
# 5. Databases > Connect > Connect your application > Copy connection string
# 6. Reemplazar <password> con tu contraseña
# Formato: mongodb+srv://usuario:password@cluster.mongodb.net/ai-interview
# MONGODB_URI=mongodb+srv://<usuario>:<password>@cluster.mongodb.net/ai-interview

# Clave secreta JWT para producción
# DEBE ser diferente a la de desarrollo
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# En Windows PowerShell: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Copiar el resultado y pegarlo en Render Dashboard
# JWT_SECRET=<generar_clave_segura_aqui>

# URL del frontend desplegado en Render
# Formato: https://nombre-de-tu-frontend.onrender.com
# Obtener después de desplegar el frontend en Render
# Se usa para configurar CORS y permitir peticiones del frontend
# FRONTEND_URL=https://ai-interview-frontend.onrender.com

# Clave de API de Google Gemini
# Misma que usas en desarrollo (frontend/.env.local)
# Obtener en: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=<tu_api_key>

# [OPCIONAL] Clave de API de OpenAI
# Solo si decides usar OpenAI en lugar de o además de Gemini
# Obtener en: https://platform.openai.com/api-keys
# 1. Crear cuenta en OpenAI
# 2. Ir a API Keys
# 3. Create new secret key
# OPENAI_API_KEY=<tu_api_key>
